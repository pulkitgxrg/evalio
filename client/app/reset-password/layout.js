export const metadata = {
  title: "Reset Password | Evalio - Create New Password",
  description: "Create a new password for your Evalio account using the reset link sent to your email.",
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
